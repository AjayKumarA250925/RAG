# Streamlit Cloud Requirements (without OCR dependencies)
streamlit==1.39.0
langchain==0.3.13
langchain-openai==0.2.14
langchain-pinecone==0.2.0
langchain-community==0.3.13
pinecone-client==5.0.1
python-dotenv==1.0.0
pypdf==4.3.1
python-docx==1.1.2
docx2txt==0.8
rank-bm25==0.2.2
numpy==1.26.4

# Note: OCR libraries (pytesseract, pymupdf) excluded for cloud deployment
# OCR requires Tesseract system installation which is not available on Streamlit Cloud
# Users should upload text-based PDFs only, or pre-process scanned PDFs locally
